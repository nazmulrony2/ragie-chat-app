`FileUpload.jsx` ফাইলটি হলো আপনার অ্যাপের **"রিসেপশন বা জমা দেওয়ার কাউন্টার"**। এখানে ইউজার তাদের ডকুমেন্ট (পিডিএফ, ছবি, অডিও ইত্যাদি) জমা দেয়, সাথে কিছু বাড়তি তথ্যও জুড়ে দিতে পারে।

আমি পুরো কোডটি সহজ বাংলায় ভেঙে ভেঙে বুঝিয়ে দিচ্ছি।

---

### ১. মেমোরি বা স্টেট (State Variables)

শুরুতেই আমরা কিছু খালি বাক্স (State) তৈরি করেছি ফর্মের তথ্য রাখার জন্য:

```javascript
const [file, setFile] = useState(null);      // আসল ফাইলটা এখানে থাকবে
const [mode, setMode] = useState('fast');    // ফাইলটা জলদি পড়বে নাকি সময় নিয়ে?
const [name, setName] = useState('');        // ফাইলের নাম
const [metadata, setMetadata] = useState(...); // বাড়তি তথ্য (যেমন: লেখকের নাম)
const [uploading, setUploading] = useState(false); // আপলোড চলছে কি না?

```

### ২. ফাইল সিলেক্ট করা এবং স্মার্ট লজিক

```javascript
const handleFileChange = (e) => {
    const selectedFile = e.target.files[0];
    // ...
    // ভিডিও বা অডিও ফাইল হলে মোড অটোমেটিক চেঞ্জ হবে
    if (fileType.startsWith('video/') ...) {
         setMode('all'); 
    }
}

```

**এখানে একটা স্মার্ট কাজ করা হয়েছে:**
সাধারণত `mode` থাকে `'fast'` (শুধু টেক্সট পড়ার জন্য)। কিন্তু ইউজার যদি ভুল করে ভিডিও ফাইল সিলেক্ট করে, `'fast'` মোডে ভিডিও চলে না।
তাই কোড অটোমেটিক চেক করে—"এটা কি ভিডিও?" যদি হ্যাঁ হয়, সে নিজে নিজেই মোড পাল্টে `'all'` করে দেয়। এটা ইউজারকে ভুল করা থেকে বাঁচায়।

### ৩. মেটাডেটা (Metadata) - স্টিকি নোটস

এই অংশটা একটু ইন্টারেস্টিং। মেটাডেটা হলো ফাইলের গায়ে লাগানো **"বাড়তি স্টিকার"**। যেমন: `Category: Finance` বা `Year: 2024`।

```javascript
const addMetadataField = () => {
    setMetadata([...metadata, { key: '', value: '', type: 'string' }]);
};

```

* **`addMetadataField`**: আপনি যতবার "+ Add Field" বাটনে চাপ দেন, মেমোরিতে একটা করে নতুন খালি ঘর তৈরি হয়।
* **`removeMetadataField`**: "✕" বাটনে চাপ দিলে ওই ঘরটা মুছে যায়।
* **`convertMetadataValue`**: ইউজার ইনপুট বক্সে সব টেক্সট হিসেবে লেখে। কিন্তু সার্ভারে পাঠানোর আগে এই ফাংশনটা সেটাকে `Number` বা `Boolean` (সত্য/মিথ্যা) এ কনভার্ট করে নেয়।

### ৪. সাবমিট করা (The Upload Process)

যখন ইউজার "Upload Document" বাটনে চাপ দেয়, তখন `handleSubmit` ফাংশন কল হয়।

**ধাপ ১: খাম তৈরি করা (FormData)**

```javascript
const formData = new FormData();
formData.append('file', file);
formData.append('mode', mode);
// ... আরও তথ্য

```

সাধারণত আমরা সার্ভারে JSON (টেক্সট) পাঠাই। কিন্তু ফাইল তো আর টেক্সট না, এটা ভারী জিনিস।
তাই আমরা **`FormData`** ব্যবহার করি। এটাকে আপনি একটা **"পার্সেল বক্স"** ভাবতে পারেন, যার ভেতরে চিঠি, ছবি, ভিডিও সব ভরা যায়।

**ধাপ ২: কুরিয়ারে পাঠানো (Axios Post)**

```javascript
const response = await axios.post('/api/documents/upload', formData, {
    headers: { 'Content-Type': 'multipart/form-data' }
});

```

* এখানে পিয়ন (`axios`) কে বলা হচ্ছে এই পার্সেলটা `/api/documents/upload` ঠিকানায় দিয়ে এসো।
* **`'Content-Type': 'multipart/form-data'`**: এটা সার্ভারকে বলে দেয়—"সাবধান! এটা সাধারণ চিঠি না, এর ভেতরে ফাইল আছে।"

**ধাপ ৩: সফল হলে (Success)**
আপলোড হয়ে গেলে আমরা ফর্মটা আবার আগের মতো খালি করে দিই (`Reset form`), যাতে ইউজার পরের ফাইল আপলোড করতে পারে।

### ৫. টেকনিক্যাল কিছু বিষয় (বিগিনারদের জন্য)

#### A. ইনপুট লুকানো (Hidden File Input)

কোডে দেখবেন ইনপুট ফিল্ডটা লুকানো আছে:

```css
/* CSS ক্লাসে hidden থাকে */
className="file-input-hidden"

```

কারণ ডিফল্ট ফাইল আপলোড বাটন দেখতে খুব পচা হয়। তাই ডেভেলপাররা আসল বাটনটা লুকিয়ে রাখে এবং তার ওপর একটা সুন্দর `label` (📁 আইকন সহ) বসিয়ে দেয়। আপনি লেবেলে ক্লিক করলে আসলে পেছনের লুকিয়ে থাকা বাটনে ক্লিক পড়ে।

#### B. আপলোডিং বাটন (Loading State)

```javascript
<button disabled={uploading}>
  {uploading ? 'Uploading...' : '📤 Upload Document'}
</button>

```

যতক্ষণ ফাইল সার্ভারে যাচ্ছে (`uploading` সত্য), ততক্ষণ বাটনটা **অচল (Disabled)** হয়ে থাকে।
কেন? যাতে অধৈর্য হয়ে ইউজার বারবার ক্লিক না করে। একবার ক্লিক করলেই যথেষ্ট!

#### C. `accept` অ্যাট্রিবিউট

```javascript
accept=".txt,.pdf,.doc,..."

```

এটা ইনপুট বক্সে বলে দেয় কোন ধরনের ফাইল আমরা গ্রহণ করব। ইউজার যখন ফাইল চুজ করতে যাবে, তখন কম্পিউটার শুধু এই টাইপের ফাইলগুলোই তাকে দেখাবে।

### সহজ সারসংক্ষেপ

এই পেজটা একটা **ডিজিটাল ডাকঘর**।
১. আপনি ফাইল ড্রপ করেন বা সিলেক্ট করেন।
২. কোড অটোমেটিক ফাইলের ধরণ বুঝে সেরা সেটিংস ঠিক করে নেয়।
৩. আপনি চাইলে ফাইলের গায়ে কিছু ট্যাগ (Metadata) লাগিয়ে দিতে পারেন।
৪. আপলোড বাটনে চাপ দিলে, কোড সব কিছু একটা বক্সে (`FormData`) ভরে সার্ভারে পাঠিয়ে দেয়।

====================================================================================================================

অবশ্যই! `FileUpload.jsx` ফাইলের আরও ৪টি টেকনিক্যাল কিন্তু খুবই গুরুত্বপূর্ণ বিষয় আমি সহজ বাংলায় বুঝিয়ে দিচ্ছি। এগুলো শিখলে আপনি রিয়্যাক্টে ফর্ম হ্যান্ডলিংয়ে ওস্তাদ হয়ে যাবেন।

### ১. ডাইনামিক ফিল্ড তৈরি করা (Dynamic Form Fields)

মেটাডেটা সেকশনে খেয়াল করুন, ইউজার যত খুশি নতুন ঘর (+ Add Field) যোগ করতে পারে আবার ডিলিটও করতে পারে। এটা কীভাবে কাজ করে?

**যোগ করা (Adding):**

```javascript
const addMetadataField = () => {
    setMetadata([...metadata, { key: '', value: '', type: 'string' }]);
};

```

* **`...metadata` (Spread Operator)**: এর মানে হলো—"পুরানো লিস্টে যা যা ছিল, সব কপি করে নিয়ে এসো।"
* **`{ key: '', ... }`**: "তারপর শেষে একটা নতুন খালি ঘর বা অবজেক্ট যোগ করো।"
* রিয়্যাক্ট যখন দেখে লিস্ট বড় হয়েছে, সে সাথে সাথে স্ক্রিনে নতুন একটা ইনপুট বক্স এঁকে দেয়।

**মুছে ফেলা (Removing):**

```javascript
const removeMetadataField = (index) => {
    setMetadata(metadata.filter((_, i) => i !== index));
};

```

* **`filter`**: এটা একটা ছাঁকনির মতো।
* লজিকটা হলো: "লিস্টের সব আইটেম রাখো, শুধু যেই আইটেমের নম্বরের (Index) সাথে ডিলিট বাটনের নম্বর মিলবে, সেটাকে ফেলে দাও।"

---

### ২. `switch` স্টেটমেন্ট (ডেটা কনভার্টার)

ইউজার ইনপুট বক্সে যাই লেখে, সেটা টেক্সট বা **String** হিসেবে জমা হয়। কিন্তু সার্ভার হয়তো সেটাকে **Number** বা **Boolean** হিসেবে চায়। এই কাজটাই `convertMetadataValue` ফাংশন করে।

```javascript
switch (type) {
  case 'number':
    return parseFloat(value) || 0;
  case 'boolean':
    return value === 'true'; // সত্য হলে true রিটার্ন করবে
  case 'array':
    return value.split(','); // কমা দিয়ে ভাগ করে লিস্ট বানাবে
  default:
    return value; // যা আছে তাই পাঠাও
}

```

**ব্যাখ্যা:**
এটা একটা ট্রাফিক পুলিশের মতো।

* সে জিজ্ঞেস করে: "তোমার টাইপ কী?"
* যদি উত্তর আসে "Number", সে টেক্সট "100"-কে গাণিতিক সংখ্যা `100`-তে রূপান্তর করে।
* যদি উত্তর আসে "Array", সে টেক্সট "apple, banana"-কে ভেঙে `['apple', 'banana']` বানিয়ে দেয়।

---

### ৩. `e.target.files[0]` (কেন `0` লিখলাম?)

ফাইল সিলেক্ট করার ফাংশনটা দেখুন:

```javascript
const selectedFile = e.target.files[0];

```

**প্রশ্ন:** আমি তো একটাই ফাইল সিলেক্ট করছি, তাহলে `[0]` বা প্রথমটা কেন বাছতে হচ্ছে?
**উত্তর:** HTML-এর ফাইল ইনপুট সিস্টেম এমনভাবে বানানো যে, এটা সবসময় একটা **লিস্ট (List/Array)** রিটার্ন করে।

* আপনি যদি ১টা ফাইল সিলেক্ট করেন, কম্পিউটার সেটাকে `[file1]` হিসেবে দেখে।
* আপনি যদি একাধিক ফাইল সিলেক্ট করেন (যদি অপশন অন থাকে), সেটা `[file1, file2]` হয়।

যেহেতু আমাদের এই অ্যাপে একবারে একটাই ফাইল নেওয়া হচ্ছে, তাই আমরা বলি—"লিস্টের প্রথম ফাইলটা (`index 0`) আমার দরকার।"

---

### ৪. এইচটিএমএল প্যাটার্ন ভ্যালিডেশন (Pattern Validation)

কোডের "Partition" ইনপুট ফিল্ডে একটা অদ্ভুত লাইন আছে:

```javascript
<input
  pattern="[a-z0-9_-]*"
  // ...
/>
<small>Lowercase alphanumeric, _ and - only</small>

```

**এটা কী?**
এটা কোনো জাভাস্ক্রিপ্ট কোড না, এটা পিওর HTML-এর ক্ষমতা।

* **`pattern="..."`**: এখানে আমরা ব্রাউজারকে একটা নিয়ম বলে দিচ্ছি।
* **`[a-z0-9_-]*`**: এর মানে হলো—"এই বক্সে শুধু ছোট হাতের ইংরেজি অক্ষর (a-z), সংখ্যা (0-9), আন্ডারস্কোর (_) এবং হাইফেন (-) লেখা যাবে।"
* ইউজার যদি বড় হাতের অক্ষর বা স্পেস দেয় এবং সাবমিট করে, ব্রাউজার অটোমেটিক লাল দাগ দেখাবে এবং বলবে—"ফরম্যাট মিলছে না।" জাভাস্ক্রিপ্ট দিয়ে কষ্ট করে চেক করার দরকার নেই, ব্রাউজার নিজেই এটা সামলে নেয়!

---

### বোনাস: কন্ডিশনাল হেল্প টেক্সট (Smart UI)

প্রসেসিং মোড (Mode) সিলেক্ট করার নিচে ছোট লেখাগুলো খেয়াল করুন:

```javascript
<small>
  {mode === 'fast' && '⚡ Fast mode: Text extraction only...'}
  {mode === 'hi_res' && '🔍 Hi-res mode: Extracts images...'}
  {mode === 'all' && '🎬 All mode: Processes all media...'}
</small>

```

**ব্যাখ্যা:**
এটা ইউজারের অভিজ্ঞতার (UX) জন্য দারুণ।

* ইউজার যখন ড্রপডাউন থেকে `fast` সিলেক্ট করে, সাথে সাথে নিচের লেখাটা পাল্টে `⚡ Fast mode...` হয়ে যায়।
* ইউজার যখন `all` সিলেক্ট করে, লেখাটা পাল্টে `🎬 All mode...` হয়ে যায়।

এটা ইউজারকে কনফিডেন্স দেয় যে সে সঠিক অপশনটা বেছে নিয়েছে।

আশা করি এই ছোট ছোট টেকনিক্যাল বিষয়গুলো এখন আপনার কাছে পরিষ্কার! এগুলো বুঝলে আপনি প্রফেশনাল মানের কোড লিখতে পারবেন।

============================================================================================================

অবশ্যই! কোডের আরও গভীরে গিয়ে ৪টি খুব গুরুত্বপূর্ণ কনসেপ্ট আমি বুঝিয়ে দিচ্ছি। এগুলো জানলে আপনি রিয়্যাক্টের "State Management" সম্পর্কে অনেক ক্লিয়ার হবেন।

### ১. মেমোরি আপডেটের নিয়ম (State Immutability)

`handleMetadataChange` ফাংশনটা একটু মন দিয়ে দেখুন। আমরা যখন মেটাডেটার কোনো একটা ইনপুট বক্সে কিছু লিখছি, তখন আমরা সরাসরি সেটা চেঞ্জ করছি না। বরং একটু ঘুরিয়ে কাজটা করছি:

```javascript
const handleMetadataChange = (index, field, value) => {
    const newMetadata = [...metadata]; // ১. পুরো লিস্টের একটা ডুপ্লিকেট কপি তৈরি
    newMetadata[index][field] = value; // ২. সেই কপিতে চেঞ্জ করা
    setMetadata(newMetadata);          // ৩. কপিটা আসল মেমোরিতে সেট করা
};

```

**কেন এমন করলাম?**
রিয়্যাক্টে একটা কড়া নিয়ম আছে: **"আসল মেমোরি (State) সরাসরি কাটাছেঁড়া করা যাবে না।"**
মনে করুন `metadata` হলো একটা **অফিসিয়াল দলিল**। আপনি দলিলে সরাসরি কলম দিয়ে কাটাকাটি করতে পারেন না।

* তাই আমরা প্রথমে দলিলটার একটা **ফটোকপি (`[...metadata]`)** করি।
* সেই ফটোকপিতে যা খুশি এডিট করি।
* শেষে বলি, "এখন থেকে এই এডিট করা কপিটাই আসল দলিল (`setMetadata`)।"

এটা না করলে রিয়্যাক্ট বুঝতে পারে না যে ডেটা চেঞ্জ হয়েছে, আর স্ক্রিনে কোনো পরিবর্তন দেখায় না।

---

### ২. ফাইল ইনপুট রিসেট করার ট্রিক (Direct DOM Manipulation)

সাবমিট সফল হওয়ার পর আমরা ফর্ম রিসেট করছি। কিন্তু একটা লাইন একটু অন্যরকম:

```javascript
setFile(null); // রিয়্যাক্টের মেমোরি থেকে ফাইল মুছলাম
document.getElementById('file-input').value = ''; // <--- এই লাইনটা কেন?

```

**ব্যাখ্যা:**
রিয়্যাক্ট অনেক শক্তিশালী, কিন্তু **ফাইল আপলোড বাটন (File Input)** একটু একগুঁয়ে।

* আপনি `setFile(null)` করলে রিয়্যাক্টের মেমোরি পরিষ্কার হয়, কিন্তু ব্রাউজারের আসল ইনপুট বক্সে ফাইলের নামটা (যেমন "cv.pdf") তখনো আটকে থাকে।
* তাই আমরা জোর করে ব্রাউজারকে বলি (`document.getElementById...`): "তোমার পেটের ভেতরে যা ফাইল ধরে রেখেছ, সেটা ফেলে দাও (`value = ''`)।"

এটা রিয়্যাক্টের নিয়মের একটু বাইরে, কিন্তু ফাইল ইনপুট পরিষ্কার করতে এটা করতেই হয়।

---

### ৩. সেফটি চেইন বা অপশনাল চেইনিং (`?.`)

এরর হ্যান্ডলিং অংশে এই অদ্ভুত চিহ্নগুলো খেয়াল করুন:

```javascript
setError(err.response?.data?.error || 'Upload failed');

```

**এটা কী?**
এটাকে বলে **Optional Chaining (`?.`)**। এটা কোড ক্র্যাশ হওয়া থেকে বাঁচায়।

* মনে করুন সার্ভার ডাউন, তাই `err.response` বলে কিছু নেই (undefined)।
* আপনি যদি `err.response.data` লিখতেন, কোড বলত: "আরে response-ই তো নাই, তার আবার data কী?"—এবং অ্যাপ ক্র্যাশ করত।
* কিন্তু `err.response?.data` লিখলে কোড বলে: "response আছে কি? না থাকলে ওখানেই থামো, সামনে এগিয়ে `data` খুঁজতে যেও না।"

সহজ কথায়, এটা হলো—**"যদি থাকে তবেই সামনে যাও, না হলে ফিরে এসো।"**

---

### ৪. পার্সেলের ভেতরে চিঠি (JSON.stringify in FormData)

ফর্ম সাবমিট করার সময় মেটাডেটা পাঠানোর লজিকটা দেখুন:

```javascript
formData.append('metadata', JSON.stringify(metadataObj));

```

**প্রশ্ন:** আমরা তো ফাইল বা টেক্সট সরাসরি `append` করছিলাম। এখানে `JSON.stringify` কেন করলাম?

**উত্তর:**
`FormData` বা আমাদের পার্সেল বক্সটা একটু সেকেলে। সে শুধু **ফাইল** আর **সাধারণ টেক্সট** বোঝে। সে জাভাস্ক্রিপ্টের স্মার্ট অবজেক্ট (যেমন: `{ year: 2024, author: "Me" }`) বোঝে না।

* তাই আমরা অবজেক্টটাকে **প্যাকেট** করি। `JSON.stringify` পুরো অবজেক্টটাকে একটা লম্বা স্ট্রিং বা টেক্সট বানিয়ে ফেলে।
* `"{ "year": 2024, "author": "Me" }"` — এখন এটা একটা টেক্সট।
* সার্ভার এই টেক্সটটা রিসিভ করে আবার প্যাকেট খুলে (Parse করে) আসল অবজেক্টটা বের করে নেয়।

আশা করি এই ৪টি কনসেপ্ট আপনার রিয়্যাক্ট জ্ঞানকে আরও পোক্ত করবে! কোনোটা বুঝতে সমস্যা হলে নির্দ্বিধায় জিজ্ঞেস করবেন।